#pragma once

#include <iostream>
#include <string>
#include <SFML/Graphics.hpp>
#include <list>
#include <fstream>
#include <math.h>

enum {
	DIREITA,
	ESQUERDA,
	CIMA,
	BAIXO,
	BLUEORB,
	REDORB,
	GREENORB,
	CHAVE,
	MOEDA
};

using namespace std;
using namespace sf;
